// Articles, tutorials and other writing. Leave empty (or all-placeholder)
// until real resources exist — the Resources section renders a "coming soon"
// state automatically when there is nothing real to show.

import type { Resource } from "./types";

export const resources: Resource[] = [];
